# Raw source data (large, re-downloadable, rebuilt into data/hpo.db by scripts/build_db.py)
raw_data/

# Uncompressed database (~42MB, exceeds Cloudflare Pages' 25 MiB per-file
# limit). Only the gzipped copy, data/hpo.db.gz, is committed/deployed --
# see scripts/build_db.py and assets/js/db.js.
data/hpo.db

# OS / editor cruft
.DS_Store
*.pyc
__pycache__/

# Local scratch
node_modules/
*.log
